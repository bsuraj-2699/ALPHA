# API base URL — points at the FastAPI app from apps/api/.
# Local dev:
NEXT_PUBLIC_API_URL=http://localhost:8000
# Production (e.g. UI on Vercel, API on Render/Railway/Fly):
# NEXT_PUBLIC_API_URL=https://your-api.example.com
